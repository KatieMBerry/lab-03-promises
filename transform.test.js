const fs = require('fs').promises;
const { transform } = require('./transform');

describe('transform fn', () => {
    it('read a file, remove all capital letters, then make all letters capital, then reverse the string', async () => {


    })
});